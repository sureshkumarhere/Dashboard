import { Inter } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });

import { Lusitana } from 'next/font/google';

export const lusitana = Lusitana({
  subsets: ['latin'],
  weight: '400', // ya '700' depending on need
});
